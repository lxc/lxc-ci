- view:
    name: Libresource
    view-type: list
    description: CGroup aware resource query library
    columns:
        - status
        - weather
        - job
        - last-success
        - last-failure
        - last-duration
        - build-button
    regex: libresource-.*
